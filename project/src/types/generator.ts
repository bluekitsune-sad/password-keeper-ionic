export type CharactersValue = {
    lowercase: string[];
    uppercase: string[];
    numbers: string[];
    specials: string[];
}
export type CheckOptionsValue = {
    lowercase: boolean;
    uppercase: boolean;
    numbers: boolean;
    specials: boolean;
}
